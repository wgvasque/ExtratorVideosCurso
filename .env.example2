# ============================================
# AUTENTICAÇÃO PARA PLATAFORMAS
# ============================================
# Credenciais são gerenciadas por domínio no arquivo credentials.json
# Configure via interface web: ⚙️ CONFIGURAÇÕES > 🔐 CREDENCIAIS POR DOMÍNIO

# ============================================
# CHAVES DE API
# ============================================
GEMINI_API_KEY=sua_chave_gemini
OPENROUTER_API_KEY=sua_chave_openrouter

# ============================================
# CONFIGURAÇÃO DE IA
# ============================================
# Habilitar OpenRouter (true = usa OpenRouter, false = usa Gemini)
USE_OPENROUTER=false

# Habilitar fallback automático (tenta múltiplos modelos até sucesso)
OPENROUTER_USE_FALLBACK=true

# Número máximo de tentativas de fallback
OPENROUTER_MAX_FALLBACK_ATTEMPTS=10

# ============================================
# MODELO DE PROMPT
# ============================================
# Templates disponíveis na pasta modelos_prompts/
# modelo2 = Padrão (equilibrado)
# modelo3 = Alternativo
# modelo4 = Avançado (P.R.O.M.P.T.)
# modelo5 = Experimental
PROMPT_TEMPLATE=modelo2

# ============================================
# WHISPER (TRANSCRIÇÃO)
# ============================================
# Modelos: small, medium, large-v3
WHISPER_MODEL=small

# Dispositivo: cpu ou cuda (GPU)
WHISPER_DEVICE=cpu

# Tipo de computação: int8 (mais leve), float16, float32
WHISPER_COMPUTE_TYPE=int8

# ============================================
# ARMAZENAMENTO E CACHE
# ============================================
SUMARIOS_DIR=sumarios
CACHE_TTL_HOURS=72

# ============================================
# CONFIGURAÇÕES AVANÇADAS (OPCIONAL)
# ============================================
# Navegador em modo invisível (true para interface web)
HEADLESS=true

# Logs
LOG_LEVEL=info
LOG_DIR=logs

# Processamento paralelo
MAX_PARALLEL_CHUNKS=2
CHUNK_SECONDS=60

# Cache de resolução
SUMARIOS_CACHE_DIR=sumarios_cache
RESOLVE_CACHE_DIR=resolve_cache

# FFmpeg
FFMPEG_RW_TIMEOUT_MS=15000
FFMPEG_PREVIEW_SECONDS=0

# Download lento (evita bloqueio de CDN)
SLOW_DOWNLOAD=0
SEGMENT_SLEEP_MS=500

# Incluir blocos temporais no prompt (false = mais rápido)
OPENROUTER_USE_BLOCKS=false

# PDF (desabilitado por padrão)
ENABLE_PDF=0
WKHTMLTOPDF_PATH=
