EMAIL=seu_email@dominio.com
SENHA=sua_senha
REFERER=https://alunos.segueadii.com.br/
SLOW_DOWNLOAD=1
SEGMENT_SLEEP_MS=500
PROXY=
LOG_LEVEL=info
LOG_DIR=logs
SUMARIOS_DIR=sumarios
SUMARIOS_CACHE_DIR=sumarios_cache
RESOLVE_CACHE_DIR=resolve_cache
CACHE_TTL_HOURS=72
CHUNK_SECONDS=90
MAX_PARALLEL_CHUNKS=2

# Seleção de Modelo de Prompt
# modelo2 = Modelo 2 (padrão, equilibrado)
# modelo4 = Modelo 4 (híbrido otimizado com framework P.R.O.M.P.T.)
PROMPT_MODEL=modelo2

# Caminhos dos arquivos de configuração de prompt
PROMPT_MODELO2_PATH=prompt_padrao.json
PROMPT_MODELO4_PATH=prompt_modelo4.json

# Ou use PROMPT_PATH para especificar um arquivo customizado (sobrescreve PROMPT_MODEL)
# PROMPT_PATH=meu_prompt_custom.json
